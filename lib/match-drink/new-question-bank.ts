import { MatchDrinkQuestion } from "./types";

// Match & Drink questions are managed in Supabase.
// This legacy export stays empty only to avoid reintroducing a local question bank.
export const NEW_QUESTION_BANK: Partial<MatchDrinkQuestion>[] = [];
